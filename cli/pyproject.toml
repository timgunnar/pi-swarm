[project]
name = "pi-cli"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
    "typer>=0.15.0",
    "rich>=13.0.0",
    "httpx>=0.28.0",
]

[project.scripts]
k3s-pi = "pi_cli.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
